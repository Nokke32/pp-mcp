# MCP Framework
mcp>=1.2.0
uvicorn>=0.23.0

# Kurs-Feed-Abruf (ariva.de, für refresh_prices)
httpx>=0.27.0

# Konfiguration
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Portfolio-Performance-Parser (vendored unter src/pp_parser)
# client_pb2.py wurde mit protoc/gencode 6.31 erzeugt und benötigt die
# passende Runtime (>= 6.31, Major 6); ältere Versionen (z.B. 4.25) brechen mit
# "cannot import name 'runtime_version'".
protobuf>=6.31.1,<7
pycryptodome==3.20.0
